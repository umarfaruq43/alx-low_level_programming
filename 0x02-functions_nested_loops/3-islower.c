#include "main.h"

/**
 * _islower -> Entry point
 * @c: a chararcter Argument
 * Describtion: Print base on 0 and 1 condition.
 * Return: return (1) and (0) Success
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
